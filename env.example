## Database Configuration (PostgreSQL)

# Replace with your actual database host (e.g., localhost, 127.0.0.1)
DB_HOST=
# Replace with your database port (default for PostgreSQL is 5432)
DB_PORT=
# Replace with your database name
DB_NAME=
# Replace with your database user
DB_USER=
# Replace with your database password
DB_PASSWORD=